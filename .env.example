AUTH_TOKEN=your_chat_bot_auth_token_here

# Optional local OpenAI-compatible judge endpoint settings.
# The scripts already have these defaults, so set them only if your endpoint differs.
JUDGE_BASE_URL=http://192.168.15.182:1234/v1
JUDGE_API_KEY=sk-no-key-required
